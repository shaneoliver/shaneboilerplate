<ul class="nav">
    {% for item in items %}
        <li class="nav-item {{ item.classes | join(' ') }}">
            <a class="nav-link" href="{{ item.link }}" target="{{ item.target }}">{{ item.title }}</a>
        </li>
    {% endfor %}
</ul>